import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fo_tese_da_79',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
